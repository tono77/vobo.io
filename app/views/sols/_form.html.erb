<%= form_with(model: sol, local: true) do |form| %>
  <% if sol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sol.errors.count, "error") %> prohibited this sol from being saved:</h2>

      <ul>
      <% sol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Título %>
    <%= form.text_area :title %>
  </div>

  <div class="field">
    <%= form.label :Descripción %>
    <%= form.text_area :desc %>
  </div>

  <div class="field">
    <%= form.label :user_solicitante %>
    <%= form.text_field :user_one_id %>
  </div>

  <div class="field">
    <%= form.label :user_aprobador %>
    <%= form.text_field :user_two_id %>
  </div>

  <div class="field">
    <%= form.label :Estado %>
    <%= form.text_field :state_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
